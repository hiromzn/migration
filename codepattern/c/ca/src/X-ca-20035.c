#include <stdio.h>
#include <stdlib.h>

/*
void func01(char *buf)
{
        int             i_data_cnt = 0;
        int             i_max_dsp_cnt;

	i_max_dsp_cnt=atoi(buf);

        if (i_data_cnt < i_max_dsp_cnt)
        {
        }
        else
        {
                printf("foo\n");
        }

}

void func02()
{
        int             i_data_cnt = 0;
        int             i_max_dsp_cnt;

        if (i_data_cnt < i_max_dsp_cnt)
        {
        }
        else
        {
                printf("foo\n");
        }

}
*/

int main(int argc, char *argv[])
{
        int             i_data_cnt = 0;
        int             i_max_dsp_cnt;

        if (i_data_cnt < i_max_dsp_cnt)
        {
        }
        else
        {
                printf("foo\n");
        }
	return(0);
}
/*
int func(int b)
{
int a;
a = a + b; // warning 20035 for use of variable a
return a;
}
*/
